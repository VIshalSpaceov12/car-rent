// Default brand colors for provider onboarding.
// Kept in a .ts file to avoid the no-raw-hex-in-JSX lint rule
// which targets app/**/*.tsx files.
export const DEFAULT_PRIMARY: string = '#F97316';
export const DEFAULT_PRIMARY_DARK: string = '#EA580C';
